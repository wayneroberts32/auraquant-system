================================================================================
AURAQUANT QUANTUM BRAIN - LOGIN CREDENTIALS
================================================================================

DEFAULT ADMIN ACCOUNT:
----------------------
Email:    wayneroberts32@outlook.com.au
Password: admin123

IMPORTANT: Change the password immediately after deployment for security!

================================================================================

API ENDPOINTS FOR AUTHENTICATION:
----------------------------------

1. LOGIN (Get Token):
   POST http://localhost:8000/api/auth/token
   
   Form Data:
   - username: wayneroberts32@outlook.com.au
   - password: admin123
   
   Returns: JWT access token

2. REGISTER NEW USER:
   POST http://localhost:8000/api/auth/register
   
   JSON Body:
   {
     "email": "new_user@example.com",
     "password": "secure_password",
     "full_name": "User Name"
   }

3. GET CURRENT USER:
   GET http://localhost:8000/api/auth/me
   
   Headers:
   - Authorization: Bearer {your_jwt_token}

4. REFRESH TOKEN:
   POST http://localhost:8000/api/auth/refresh
   
   Headers:
   - Authorization: Bearer {your_jwt_token}

5. LOGOUT:
   POST http://localhost:8000/api/auth/logout
   
   Headers:
   - Authorization: Bearer {your_jwt_token}

================================================================================

TESTING LOGIN WITH CURL:
-------------------------

# Get access token:
curl -X POST "http://localhost:8000/api/auth/token" \
     -H "Content-Type: application/x-www-form-urlencoded" \
     -d "username=wayneroberts32@outlook.com.au&password=admin123"

# Use token for authenticated requests:
curl -X GET "http://localhost:8000/api/auth/me" \
     -H "Authorization: Bearer {your_token_here}"

================================================================================

SECURITY RECOMMENDATIONS:
--------------------------
1. Change the default password immediately after first login
2. Use a strong password with mixed case, numbers, and symbols
3. Consider enabling 2FA (two-factor authentication) in production
4. Rotate JWT secret keys regularly
5. Use HTTPS in production
6. Implement rate limiting for login attempts

================================================================================

TO CHANGE PASSWORD AFTER DEPLOYMENT:
-------------------------------------
1. Login with current credentials
2. Access user settings (to be implemented)
3. Or directly update in database:
   - Hash new password using bcrypt
   - Update user record in MongoDB

================================================================================